// config.js

// app requires a config.js file to connect to the API
// APIURL is the API endpoint
// APITOKEN is the JWT auth token provided when logging a user in (does not expire)

export function APIURL() {
    return 'url';
};

export function APITOKEN() {
    return 'token';
};


